#include <stdio.h>
/**
 * main - prints its name, followed by a new line.
 * @argc: int .
 * @argv: array that contains the progra.
 * Return: 0 - success.
 */
int main(int argc, char const *argv[])
{
	(void)argc;
	printf("%s\n", argv[0]);
	return (0);
}
